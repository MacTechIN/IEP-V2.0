# IEP v2.0 (Investigate Enablement Platform) — 작업 규칙

이 파일은 매 세션 자동으로 읽힌다. **여기 적힌 것은 예외 없이 지킨다.**

## 이 저장소가 무엇인가 (2026-09-01)

**대한민국 경찰 수사업무 지원 음성분석 시스템.** LEP v2.1(변호사) 에서 갈라졌고,
LEP 은 SEP v2(영업) 에서 갈라졌다. **셋은 완전 독립 제품**이다 — 자원도 배포도 섞지 않는다
(IEP: `iep-api`·`iep-web`·`iep-uploads`, 별도 Neon, 별도 저장소 MacTechIN/IEP-V2.0).

개발 계획: `docs/2026-09-01-개발계획.md`.

## IEP 의 대전제 — 절대 어기지 않는다

1. **거짓말 탐지·심리 판정을 만들지 않는다.** 「이 사람 거짓말」·점수·감정 추론 없음.
   작동하지 않고(NRC 2003), 수사에서 오판·강압을 낳고, 법정에서 깨진다.
   대신 **모순·미확인 주장·태도 변화를 원문 인용으로 짚는다** — 사람이 아니라 문장을 가리킨다.
2. **모든 분석·코칭은 「참고 자료」다.** 조사 진행·판단을 대신하거나 강제하지 않는다.
   시스템은 짚고, 조사관이 정한다. 화면·보고서에 「참고」로 명시한다.
3. **호칭을 정확히 구분한다.** 수사관/피의자/참고인/피해자/목격자. 잘못 붙으면 조사 전체가
   틀린다. 역할은 「추정」이고 사람이 고칠 수 있으며(사람이 고친 건 AI 가 안 덮음),
   확신 없으면 「미상」으로 둔다 — **틀린 호칭보다 빈 호칭이 낫다.**
4. **피해자 조사에는 압박성 코칭·모순 짚기를 걸지 않는다.** 2차 피해 방지.
5. **녹음 전에 고지·동의.** 동의 없이 녹음 버튼이 안 눌린다. 누가·언제 고지·동의했는지 남긴다.

이 다섯은 `docs/2026-09-01-개발계획.md` §0·§6 에 근거가 있다. 코드가 이걸 어기면 그 코드가 틀렸다.

## 버전 관리

기능이나 수정을 **배포할 때마다** 다음 셋을 한 묶음으로 한다. 하나라도 빠지면 배포가 끝난 것이 아니다.

1. **버전 올리기** — `worker/package.json` · `frontend/web/package.json` · `stream-service/package.json`
   셋은 **같은 번호로 함께** 움직인다 (한 제품의 세 조각이다)
2. **`CHANGELOG.md` 맨 위에 항목 추가**
3. **주석 태그** — `git tag -a vX.Y.Z -m "…"` (원격이 생기면 `--follow-tags` 로 푸시)

### 번호 규칙

LEP 는 **2.1.0 에서 시작한다** (사용자 지정). SEP v2(2.x)·SEP v1(0.x)과
**번호가 겹쳐 보여도 서로 무관하다** — 제품이 다르면 번호도 다른 세계다.

| 무엇이 | 어디를 |
|---|---|
| 기능이 늘었다 | 마이너 — `2.1.0` → `2.2.0` |
| 버그만 고쳤다 | 패치 — `2.1.0` → `2.1.1` |
| 호환이 깨진다 | 메이저 — `2.x` → `3.0.0` |

### 무엇을 적는가

CHANGELOG 와 태그 메시지에는 **무엇이 고장나 있었고 무엇이 고쳐진 것을 증명하는지**를 적는다.
실측값을 넣고, 커밋 해시를 함께 적고, 마이그레이션을 포함하면 번호를 적는다
(**코드 태그로는 DB 가 되돌아가지 않는다**).

## 계정과 로그인 잠금 (2026-08-28 확정)

SEP 에서 관리자 로그인이 안 돼 반나절을 잃었다 — 시드가 일부러 잠가 둔
계정이었는데 그 사실이 마이그레이션 주석에만 있었고, 비밀번호를 맞혀 보는
사이 잠금이 겹쳐 429 가 진짜 원인(401)을 가렸다. LEP 도 같은 잠금 구조다.

1. **비밀번호를 모르면 맞혀 보지 않는다.** 틀릴 때마다 잠금이 길어진다
   (5회부터 1→2→4→8→최대 15분). 맞혀 보는 대신
   `worker/tools/reset-password.mjs` 로 바로 재설정한다 — **잠금도 함께 푼다.**
   연결 문자열은 Neon 콘솔 → LEP 프로젝트(`ep-broad-feather`) → Connect.
2. **일부러 잠근 계정·쓸 수 없는 계정은 이 파일에 적는다.** 주석은 지켜지지 않는다.
3. **SEP 와 LEP 는 DB 도 계정도 비밀번호도 다르다.** 한쪽 값을 다른 쪽에 넣지 않는다.
4. **복구 경로는 로그인 없이 동작해야 하고, 미리 검증해 둔다.**
   이 도구는 시험 계정으로 실제 로그인 200 까지 확인돼 있다 (최소 길이: LEP 6자·SEP 9자).

로그인되는 계정 (2026-08-28 기준, 비밀번호는 적지 않는다):
`admin@company.com`(admin) · `kjv5555@naver.com`

## 배포 전 확인

- `cd worker && npx tsc --noEmit`
- `cd frontend/web && npx tsc --noEmit && npm run build:prod`
  **`npm run build` 는 localhost 를 가리키는 번들을 만든다. 반드시 `build:prod`.**

## 배포 (2026-08-30 부터 Actions)

```
gh workflow run Deploy --repo MacTechIN/LEP-V2.1
```

타입 검사 → `build:prod` → **번들 주소 확인**(`check-bundle.mjs`) → Worker → 웹 →
**올라간 번들 대조** → 태그까지 한 번에 한다. Worker 를 먼저 올린다.
대조가 5분 안에 안 맞으면 실패로 끝난다 — 배포된 줄 알고 넘어가는 것을 막는다.

**왜 손 배포를 접었나** — `wrangler deploy` 를 셸에서 돌리면 Cloudflare 토큰이
매번 세션 기록에 남는다(2026-08-29 회전한 날 454회 재노출). Actions 는 토큰을
`CLOUDFLARE_PAGES_TOKEN`(쓰기 전용 Secret)에만 둔다. `docs/2026-08-30-…` 참고.

손으로 해야 하면 아래가 그대로 남아 있다. 다만 **토큰이 셸에 남는다는 것을 알고 쓴다.**

## DB 작업

LEP 의 Neon 프로젝트는 `ep-broad-feather`(SEP 의 `ep-steep-sky` 와 **다른 프로젝트**).
접속 문자열은 GitHub Secret `DATABASE_URL` 과 워커 시크릿에 있다. SEP 처럼 `DB` 워크플로에
LEP 접속 문자열을 시크릿으로 넣고 그걸로만 작업한다 — 접속 문자열을 로컬에 두지 않는다.
`database/` 의 마이그레이션 001–015 는 SEP 스키마 그대로다. 새 DB 에는 001 부터 차례로 적용한다.

`worker/` 는 `"type": "module"` 이다 — `worker/tools/` 에 `require` 를 쓰면 CI 에서만 터진다.

## 문서를 늘리지 않는다

SEP 에서 문서가 153개까지 늘어난 뒤 대부분이 낡았다. 같은 실수를 반복하지 않는다.

| 종류 | 어떻게 |
|---|---|
| **그날의 기록** (검증·사고·작업기록) | 파일명에 날짜를 넣는다 — `2026-08-25-….md`. 낡는 것이 정상이고 관리하지 않는다 |
| **살아 있어야 하는 것** | 아래 넷 안에 들어가는지 본다. 안 들어가면 만들지 않는다 |

살아 있는 문서는 넷뿐이다: `CLAUDE.md` · `CHANGELOG.md` · `README.md` · `docs/ARCHITECTURE.md`

`docs/` 의 2026-08-25 이전 날짜 문서들은 **SEP 시절의 기록**이다 — 판단 근거로 남겨 두되
LEP 의 현재를 말한다고 믿지 않는다.

**아키텍처가 바뀌는 변경**(마이그레이션·새 서비스·배포 대상 변경)이면
`docs/ARCHITECTURE.md` 도 **같은 커밋에서** 고친다.

## 지우는 일

**자동으로 지우는 청소기를 만들지 않는다.** 목록을 보여 주고 사람이 정한다.
되돌릴 수 없는 삭제 전에는 **목록과 사본을 남긴다.**
