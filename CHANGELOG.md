# 변경 기록 — IEP (Investigate Enablement Platform)

이 제품은 [유의적 버전](https://semver.org/lang/ko/)을 따른다.
규칙과 배포 절차는 [CLAUDE.md](CLAUDE.md), 개발 계획은 [docs/2026-09-01-개발계획.md](docs/2026-09-01-개발계획.md).

무엇이 고장나 있었고 무엇이 고쳐진 것을 증명하는지를 적는다. 실측값이 있으면 넣는다.

---

## [0.3.1] — 2026-09-01 — 사용자 가이드 IEP 재작성 · 조서 날짜 버그 · 모바일 로그인

- **사용자 가이드(guide.html) 전면 재작성** — LEP(상담·소장·변호사·요건사실 924줄)를 걷어내고
  IEP 현재 기능으로 다시 씀: 조사 종류 5종, 진술 분석(모순·미확인·태도변화·미응답 + 인용 대조),
  화자 구분(미상), 조서·수사보고(모델 없이 조립·초안), 수사사건 연결, 소속 기관(경찰/해양경찰),
  §0 원칙(점수 없음·지어내지 않음). 디자인(CSS·다크모드)은 유지. 남은 LEP 용어 0.
- **조서·수사보고 날짜 버그** — 「조사 일시」가 「Tue Sep 01」로 나오던 것 수정.
  `gather()` 가 pg 의 Date 객체를 `String()` 해서 `slice(0,10)` 이 요일이 되던 것을
  `new Date(..).toISOString().slice(0,10)` 으로 고침 → 「2026. 9. 1.」.
- **모바일 로그인 후 화면 안 바뀜** — 로그인 성공 시 SPA `navigate('/')` 가 일부 모바일/인앱
  브라우저에서 안 붙던 것을, 토큰이 이미 저장되므로 하드 이동(`window.location.assign('/')`)으로 변경.

---

## [0.3.0] — 2026-09-01 — 전면 IEP 전환 · §0 영업 요소 제거 · 경찰/해양경찰

**첫 화면부터 LEP 였다.** iep-web 인데 로고·홈·통계·용어가 LEP(변호사·영업) 그대로였다.
(확인: `lep-web.pages.dev`=진짜 LEP(lep-api), `iep-web.pages.dev`=IEP(iep-api) — 별개 제품.)

- **브랜딩**: 로고 `LEP`→`IEP`, `index.html` 제목 → IEP - Investigate Enablement Platform,
  `guide.html` 브랜딩 문구.
- **홈(§0)**: 영업 카드 제거 — 「평균 점수」·「내 성과」·「최근 점수 추이」·「팔로업 이메일 대기」.
  「이번주 미팅」→「이번주 조사」, 최근 목록의 점수 열 제거. 코칭·오늘 할 일·최근 조사는 유지.
- **통계(§0)**: 「나의 성과」 점수판(현재·주간·월간 점수·팀 순위·대상자 만족도)을 걷어내고
  **점수 없는 활동 요약**(이번주 조사·확인 항목 처리율)으로. "IEP 는 수사관을 점수로 평가하지 않는다".
- **용어 일괄 전환**: 미팅·상담→조사, 의뢰인·고객→대상자, 변호사→수사관 (12개 파일).
  한글 조사 호응까지 교정(조사이→조사가, 수사관가→수사관이 등 — 받침 변화 양방향).
- **조사 종류 칩**: legal/business → 피의자 신문·참고인 조사·피해자 조사·면담·회의.

**경찰 / 해양경찰 분리** — 계급(순경~경정)은 두 기관이 같지만 담당 분야가 다르다.
프로필에 「소속 기관」 선택을 넣고 담당 분야를 기관별로 전환(경찰: 경제·지능·사이버…,
해양경찰: 해양오염·해상안전·해양범죄·밀수/밀입국…). 마이그레이션 **034** (`users.agency`, 멱등).

남음: `guide.html` 본문은 변호사·상담 내용이라 별도 문서 작업 필요.

---

## [0.2.0] — 2026-09-01 — 진술 분석(S4) · 수사 서식(S6) · 수사관 UI

**S4 진술 분석 — 핵심 가치.** LEP 의 「법률 분해」 step 을 「진술 분석」으로 교체
(`worker/src/workflows/analysis.ts`). 조사 녹취에서 판정하지 않고 **근거를 짚는다** (§0):
- `statementAnalysis.ts` 신규 — CONTRADICTION(모순)·UNANSWERED(미응답)·UNVERIFIED(미확인)·
  ATTITUDE_SHIFT(태도 변화)를 **원문 인용과 함께**. 거짓말·심리·점수를 판정하지 않는다.
- **인용 대조 두 겹.** (1) 서비스가 모델 인용을 전사와 대조해 없으면 버림, (2) 마이그레이션
  **033** 이 DB 에서 CONTRADICTION·UNANSWERED 의 refs≥2 를 못박음. 실측: 지어낸 인용을 섞은
  5개 findings 중 **3개(가짜 인용 포함)를 버리고 진짜 근거 2개만 통과**.
- 피해자 조사(victim)는 모순 짚기를 하지 않음 — 2차 피해 방지 (§6-A).
- PII 를 가린 본문으로 분석하고, 저장 시 인용을 `unmaskPii` 로 되돌려 원문 근거를 남김 (021).
- 화자 라벨 전사로 분석 → 인용에 수사관/대상자 호칭 유지 (§6-D).

**S6 수사 서식 — 소장을 조서로.** `documents/complaint.ts`(소장, LEP)를 등록부에서 내리고
`documents/investigator.ts` 신규 3종 등록:
- **진술조서**(참고인·피해자·목격자) · **피의자신문조서**(피의자, 진술거부권·변호인조력권 고지 포함) ·
  **수사보고**(조사 요지 + 진술 분석 요지).
- **모델 없이 조립**(assemble) — 전사·인적사항·고지문을 짜맞출 뿐 한 문장도 지어내지 않음 (§0).
  `generate.ts` 의 `gather()` 를 조사(meeting) 중심 자료(전사·대상자·분석)까지 모으게 확장.

**LEP 잔재 제거 — 수사관 UI.**
- 내 프로필: 변호사 정보(등록번호·지방변호사회·법무법인·취급분야) → **수사관 정보**
  (계급/직위·소속 관서·담당 분야). 작성자 표시가 조서·수사보고에 쓰임.
- 새 사건: 청구원인(채무불이행·불법행위·부당이득)·이해충돌(의뢰인·원고·소장) →
  **죄명/혐의·분야·관련자 확인**. 중복 인물 검사 기능은 유지(다른 사건 중복 확인).
- 조사 상세: **진술 분석 탭 신설** — findings 를 인용(refs)·확인 질문과 함께 표시.
  점수(severity)는 보이지 않음 (§0). 서면 만들기 문구를 조서·수사보고로 교체.

마이그레이션: **033_iep_statement_findings.sql** (findings kind 확장 + 인용 대조 DB 제약). 멱등.

---

## [0.1.0] — 2026-09-01 — 프로젝트 착수 (S0)

LEP v2.1 에서 갈라져 경찰 수사 지원 플랫폼으로 시작. **SEP·LEP 와 완전 독립.**

- LEP 소스 복제(worker·frontend·stream·database·docs). `node_modules`·전과 폴더·`.git` 제외
- 인프라 이름 전부 iep- 로 치환 (`iep-api`·`iep-web`·`iep-uploads`·`iep-analysis`·`iep-transcribe`)
- LEP Cloud Run 스트림 주소 제거 — IEP 스트림은 S5 에서 구축, 그전까지 자막 비활성
- `check-bundle` 자막 검사를 S5 까지 보류 (아직 없는 주소를 요구하면 배포가 막힌다)
- CLAUDE.md 를 IEP 대전제 다섯으로 새로 씀 — 거짓말 탐지 금지·참고자료 원칙·호칭 구분·
  피해자 보호·고지 동의. LEP 의 검증된 규율(배포·게이트·백업)은 이어받음
- 버전 0.1.0 초기화, git 저장소 MacTechIN/IEP-V2.0 연결

**아직 코드 동작은 LEP 그대로다.** 도메인 치환(변호사→수사관, 소장→조서)과 새 서비스
(진술분석·절차점검·코칭요약)는 S1 이후. 이 커밋은 「독립된 빈 골격」이다.
